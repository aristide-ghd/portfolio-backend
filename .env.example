# Server
PORT=3000

# Mailer (set to "console" to log messages instead of sending)
MAILER=console
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=your_smtp_user
MAIL_PASS=your_smtp_password
SITE_OWNER_EMAIL=owner@example.com
 
# MongoDB (optional). If set, contact submissions will be persisted.
MONGO_URI=mongodb://localhost:27017/portfolio
